<%- include('../includes/head.ejs') %>

<div class="bg-shapes">
  <div class="shape shape-1"></div>
  <div class="shape shape-2"></div>
  <div class="shape shape-5">★</div>
  <div class="shape shape-6">★</div>
</div>

<%- include('../includes/navbar.ejs') %>

<main class="page-wrapper">
  <div class="container">
    
    <div class="page-hero">
      <div class="page-hero-label">— Browse</div>
      <h1 class="page-hero-title">All <span>Games</span></h1>
      <div class="page-hero-subtitle">Discover, rate, and review your favourites from our curated collection</div>
    </div>

    <div class="games-toolbar">
      <div class="search-box">
        <span class="search-icon">🔍</span>
        <input class="search-input" type="text" placeholder="Search games, genres, developers..." />
      </div>
      <div class="filter-tags">
        <button class="filter-tag active" data-filter="all">All</button>
        <button class="filter-tag" data-filter="rpg">RPG</button>
        <button class="filter-tag" data-filter="action">Action</button>
        <button class="filter-tag" data-filter="racing">Racing</button>
        <button class="filter-tag" data-filter="strategy">Strategy</button>
        <button class="filter-tag" data-filter="horror">Horror</button>
        <button class="filter-tag" data-filter="fighting">Fighting</button>
        <button class="filter-tag" data-filter="roguelike">Roguelike</button>
        <button class="filter-tag" data-filter="sports">Sports</button>
      </div>
    </div>

    <div class="games-meta-row">
      <div id="games-count" class="games-count">Showing <%= game ? game.length : 0 %> games</div>
      <div class="sort-control">
        <span class="sort-control-label">Sort:</span>
        <select class="form-select">
          <option>Rating: High to Low</option>
          <option>Newest First</option>
          <option>Most Reviewed</option>
          <option>A → Z</option>
        </select>
      </div>
    </div>

    <div class="grid-4 games-grid" id="games-grid">
      <% if (game && game.length > 0) { %>
      <% game.forEach(g => { %>
      <div class="game-card" data-title="<%= g.title %>" data-genre="<%= g.genre %>" data-developer="<%= g.developer %>">
        <div class="game-card-img game-card-img--poster">
          <img src="<%= g.imageUrl || 'https://via.placeholder.com/600x900' %>" alt="<%= g.title %>" />
          <% if (g.badge) { %>
          <span class="game-card-badge game-card-badge--featured"><%= g.badge %></span>
          <% } %>
        </div>
        <div class="game-card-body">
          <div class="game-card-meta"><%= g.platform %> • <%= g.year %></div>
          <h3 class="game-card-title"><%= g.title %></h3>
          <div class="game-card-sub"><%= g.genre %> | <%= g.developer %></div>
          <p class="game-card-desc"><%= g.description %></p>
          <a href="/game-details/<%= g._id %>" class="game-card-btn">View Game</a>
        </div>
      </div>
      <% }) %>
      <% } else { %>
      <div class="games-empty-state grid-span-all">
        <div class="games-empty-state-icon">👾</div>
        <p class="games-empty-state-text">No games found. Check back later!</p>
      </div>
      <% } %>
    </div>

    <button class="back-to-top" id="backToTop" aria-label="Back to top">↑</button>

    <% if (game && game.length > 0) { %>
    <div class="load-more-wrap">
      <button class="btn btn-outline btn-lg" onclick="this.textContent='Loading...';setTimeout(()=>this.textContent='Load More Games →',1500)">Load More Games →</button>
    </div>
    <% } %>

  </div>
</main>

<%- include('../includes/footer.ejs') %>
<%- include('../includes/end.ejs') %>